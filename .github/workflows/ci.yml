name: CI

on:
  push:
    branches: ["**"]
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r backend/requirements.txt -r requirements-dev.txt

      - name: Lint (ruff)
        run: ruff check ml pipeline mlops scripts tests

      - name: Run tests
        run: pytest -q

      - name: Smoke-test the data pipeline (offline, reproducible)
        run: |
          python -m scripts.init_warehouse
